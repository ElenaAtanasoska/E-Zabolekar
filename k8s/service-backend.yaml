apiVersion: v1
kind: Service
metadata:
  name: dental-backend-service
spec:
  selector:
    app: dental-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: LoadBalancer